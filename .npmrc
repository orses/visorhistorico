ignore-scripts=true
# vite-plugin-pwa@1.2.0 aún declara peer vite ^3–^7; usamos vite 8 por
# seguridad (GHSA server.fs.deny). El override es seguro porque en runtime
# solo usa APIs estables del plugin system de Vite.
legacy-peer-deps=true
